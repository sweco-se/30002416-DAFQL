# daf_gqlquery

This is a Python package containing the code for the DafGQLQuery transformer.
It belongs to the FME Package `pesd.daf-query`.

The Python code here is only to be used by the pesd.daf-query package.
